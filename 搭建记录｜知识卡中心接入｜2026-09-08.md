# 搭建记录｜知识卡中心接入｜2026-09-08

## 背景

用户反馈：候选知识卡和调用日志藏在知识流水线深处，工作台首页看不到，不知道在哪里。需要把这些内容展示在更显眼的位置。

同时，任务卡 151 已建立，任务卡与知识库同步。

## 本次改动

### 1. 知识卡调用器 Skill 创建与迭代

- **v0.1**：创建轻量候选知识卡调用 Skill，用 5 个真实问题测试（企业为什么需要 Ontology、什么情况下用 Workflow 而非单 Agent、多 Agent 架构如何选择、企业 AI 项目为什么容易失败、RAG/知识库/业务数据库/记忆系统的区别）
- **v0.1.1**：补读剩下 3 篇源专题，增加主依据/辅助依据/参考的区分标准，把调用反馈单独记入调用日志
- **v0.1.2**：全部中文化（名称、字段、术语、目录）
- **v0.2.0**：进入全局 Skills，改名为"知识卡调用器"（去掉"候选"，支持各类知识卡的调用），调用日志改名为"知识卡调用日志.md"

**Skill 位置**：
- 豆包全局：`/Users/xucheng/Library/Application Support/DoubaoWork/Profile 10/.doubaowork/agent_mode/workspace/.user_skills/知识卡调用器/SKILL.md`
- WorkBuddy：`~/.workbuddy/skills/知识卡调用器/SKILL.md`

### 2. AI 工作台接入候选知识卡和调用日志

**问题**：候选知识卡和调用日志没有被编译进工作台，原因有二：
1. 这些文件是在最后一次编译（22:19）之后创建的
2. "知识卡调用日志.md" 因为文件名包含"日志"被排除模式过滤掉了

**解决方案**：
- 修改 `generate.js`，添加白名单 `WHITELIST_PATTERNS`，把"知识卡调用日志"、"候选知识卡"、"知识卡片与Agent调用标准"从排除模式中豁免
- 添加 `isExcluded()` 函数，白名单优先：命中白名单的文件即使命中排除模式也强制发布
- 修改 `readMDFiles()` 和 `readMDFilesRecursive()` 函数，使用新的 `isExcluded()` 函数

### 3. 知识卡中心首页展示

**问题**：即使候选知识卡和调用日志被编译进工作台，也藏在知识流水线专题深处，首页看不到。

**解决方案**：
- 修改 `generate.js`，专门提取候选知识卡、调用日志、调用标准和评估记录的数据，添加到 `data.knowledgeCardCenter` 对象中
- 修改 `index.html`，在首页"知识流水线 · 底座"和"原始资料 / 养料"卡片之后，新增"知识卡中心"区域，包含：
  - 知识卡中心入口卡片（紫色渐变）：显示总数，点击进入详情页
  - 候选知识卡列表：最近 3 张，直接点击阅读
  - 调用日志列表：最近 2 条，直接点击阅读
- 新增 `showKnowledgeCardCenter()` 函数，点击入口卡片进入知识卡中心详情页，展示所有候选知识卡、调用日志、调用标准和评估记录

### 4. 部署记录

| 时间 | 提交 | 说明 |
|---|---|---|
| 2026-09-08 23:42 | f349af8 | 首次编译，新增候选知识卡和调用日志（push 失败，网络问题） |
| 2026-09-08 23:50 | 27d2532 | 重新编译并部署成功，知识流水线 53 篇 |
| 2026-09-09 00:05 | 0a59edf | 新增知识卡中心首页展示，7 个文件变更，208 行新增 |

## 文件变更清单

### generate.js
- 新增 `WHITELIST_PATTERNS` 白名单数组
- 新增 `isExcluded()` 函数
- 修改 `readMDFiles()` 和 `readMDFilesRecursive()` 使用 `isExcluded()`
- 新增知识卡中心数据提取逻辑（`knowledgeCards`、`callLogs`、`callStandards`、`callEvaluations`）
- 在 `data` 对象中新增 `knowledgeCardCenter` 字段

### index.html
- 在首页 `renderHome()` 函数中新增知识卡中心区域
- 新增 `showKnowledgeCardCenter()` 函数，展示知识卡中心详情页

### 新增文件
- `搭建记录｜知识卡中心接入｜2026-09-08.md`（本文件）

## 当前状态

- ✅ 知识卡调用器 Skill v0.2.0 已进入全局 Skills，已分发给 WorkBuddy
- ✅ AI 工作台已接入候选知识卡和调用日志，部署成功
- ✅ 知识卡中心已在首页展示，点击可进入详情页
- ✅ 反馈自动回写已在 Skill 中定义，Agent 使用时自动执行
- ⏸️ 候选知识卡扩展到其他领域（暂缓，先把当前的 AI Agent 工程知识领域跑通）

## 下一步

1. 用真实任务测试知识卡调用器 Skill，看看在实际使用中是否顺畅
2. 根据测试结果修正 Skill
3. 考虑把知识卡调用器 Skill 整理成共享版本，分发给其他 Agent
4. 候选知识卡扩展到其他领域（如企业 AI、FDE、政策研究等）

## 相关链接

- 知识卡调用器 Skill（豆包全局）：`/Users/xucheng/Library/Application Support/DoubaoWork/Profile 10/.doubaowork/agent_mode/workspace/.user_skills/知识卡调用器/SKILL.md`
- 知识卡调用器 Skill（WorkBuddy）：`~/.workbuddy/skills/知识卡调用器/SKILL.md`
- 候选知识卡试点：`/Users/xucheng/Documents/c 徐的知识库/知识流水线/候选知识卡试点｜AI Agent工程知识｜2026-09-08.md`
- 知识卡调用日志：`/Users/xucheng/Documents/c 徐的知识库/知识流水线/知识卡调用日志.md`
- 知识卡片与 Agent 调用标准：`/Users/xucheng/Documents/c 徐的知识库/知识流水线/知识卡片与Agent调用标准.md`
- AI 工作台 GitHub 仓库：https://github.com/xuchengppp-source/ai-knowledge-workbench.git
